<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Applications</title>
    <%-include('cdn.ejs')%>
  </head>
  <body>
    <h1 class="my-5 text-center text-success">
      This is the data of all the applications so far:
    </h1>
    <div class="container">
      <div class="mt-3 mb-5 px-3 text-center w-100">
        <a href="/" class="btn btn-primary fw-bold">Click here to go back to add new application</a>
      </div>
      <table class="table table-striped table-striped-columns table-hover">
        <thead>
          <tr>
            <th scope="col" class="bg-secondary">Sr. No.</th>
            <th scope="col">ID</th>
            <th scope="col">Name</th>
            <th scope="col">Email</th>
            <th scope="col">Experience</th>
            <th scope="col">About the candidate</th>
            <th scope="col">Resume File</th>
            <th scope="col">Edit/Delete</th>
          </tr>
        </thead>
        <tbody>
          <% data.map((val,index)=>{%>
          <tr>
            <th scope="row" class="bg-secondary"><%-index+1%></th>
            <td><%-val.id%></td>
            <td><%-val.name%></td>
            <td><%-val.email%></td>
            <td><%-val.experience%></td>
            <td><%-val.message%></td>
            <td><%-val.file%></td>
            <td>
              <a
                href="/edit?id=<%-val.id%>"
                onclick="return confirm('Do you want to edit this application?')"
                class="btn text-warning"
                >&#9998;</a
              >
              <button
                onclick="return confirm('Do you want to delete this application?')"
                class="btn"
              >
                <a href="/delete/<%-val.id%>" class="text-danger"
                  ><i class="fa fa-trash"></i
                ></a>
              </button>
            </td>
          </tr>
          <%})%>
        </tbody>
      </table>
    </div>
  </body>
</html>
